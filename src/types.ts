/**
 * Represents an object that contains parsed information from a Wiktionary page.
 */
export interface Page {
  /**
   * The headword of the page (i.e. the word that is defined on the page).
   */
  headword: string;
}
